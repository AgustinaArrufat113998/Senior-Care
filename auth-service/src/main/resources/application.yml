server:
  port: 8082

spring:
  application:
    name: auth service
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

jwt:
  secret: clave-Super-Segura-Para-App-Senior-Care-123!
  expiration: 3600000

user:
  service:
    url: http://localhost:8081/api/users
    findByEmail: /by-email?email=
