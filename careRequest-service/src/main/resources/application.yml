server:
  port: 8083

spring:
  application:
    name: care-request-service

  datasource:
    url: jdbc:mysql://localhost:3306/care_request_db?useSSL=false&serverTimezone=UTC
    username: root
    password: tu_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
