server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    # Use mysql-db:3306 when running inside Docker network
    url: jdbc:mysql://localhost:3307/seniorcare_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: senior
    password: senior123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
